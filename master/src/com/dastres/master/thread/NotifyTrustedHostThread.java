package com.dastres.master.thread;


public class NotifyTrustedHostThread extends Thread {

//	private boolean masterSlave;

	public NotifyTrustedHostThread(String responseRule) {
//		setPriority(Thread.MIN_PRIORITY);
//		masterSlave = SystemSettingAction.RESPONSE_RULE_MASTER_SLAVE
//				.equals(responseRule);
	}

	@Override
	public void run() {
//		try {
//			List<SlaveTO> activeSlaves = ServiceFactory
//					.getSlaveManagementService().getActiveSlaveTO();
//			Set<String> hosts = new HashSet<String>();
//			for (SlaveTO slv : activeSlaves) {
//				hosts.add(new URL(slv.getAddress()).getHost());
//			}
//		} catch (Throwable e) {
//			e.printStackTrace();
//		}
	}
}
